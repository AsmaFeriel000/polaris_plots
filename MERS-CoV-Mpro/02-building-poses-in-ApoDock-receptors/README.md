## Docking the molecules onto ApoDock-modelled receptors using FEgrow

Activate the fegrow environment using:

``` conda env create -f fegrow-env.yaml```

``` conda activate fegrow-env ```

Then run the script:

``` python auto-fegrow.py ```
